services:
  autoindex:
    build: .
    image: autoindex
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /path/to/files:/data:ro # change to the directory you want to serve
    # command: ["-tree"]        # extra flags, e.g. -tree or -all
